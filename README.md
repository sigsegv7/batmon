# Batmon - a battery monitor daemon

## What is this?

This is a simple program for Linux to throttle the processor when the battery is too low, as well as
initiating a controlled sync + shutdown if critical.
